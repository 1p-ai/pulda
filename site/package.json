{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "qa:check": "node scripts/qa-check.mjs",
    "qa": "npm run build && npm run qa:check",
    "test:e2e": "npm run build && playwright test",
    "postinstall": "playwright install --with-deps"
  },
  "dependencies": {
    "@portabletext/to-html": "^2.0.13",
    "astro": "^4.12.2",
    "astro-icon": "^1.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.3",
    "@types/node": "^20.14.12"
  }
}
