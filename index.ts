import blockContent from './blockContent'
import project from './project'
import story from './story'
import siteSettings from './siteSettings'

export const schemaTypes = [project, story, siteSettings, blockContent]